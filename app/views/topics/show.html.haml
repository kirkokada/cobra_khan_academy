%aside.col-md-3
  %h2
    = @topic.name.titleize
    - if @topic.parent
      %span.parent_topic_name
        ="(#{link_to @topic.parent.name, @topic.parent})".html_safe
  %p
    %em= @topic.description
  .list-group
    - @topic.children.each do |child|
      .list-group-item= link_to child.name, topic_path(child)
.col-md-9
  .container
    